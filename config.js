/*
  Set this to your deployed backend's URL (no trailing slash), e.g.
  after deploying to Render it might be:
    const API_BASE = "https://anokhi-silks-backend.onrender.com";

  Leave it as an empty string to run the storefront in offline/demo
  mode: products come from the bundled products.js, and checkout only
  offers Cash on Delivery, saved to the browser only (nothing is sent
  anywhere) — same behavior as before the backend existed.
*/
const API_BASE = "http://localhost:4000";
